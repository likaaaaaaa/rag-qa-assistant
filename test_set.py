# 基于 211 段知识库(data/kb_01~11 + sample.txt)的 39 个测试问题
# 四类：A 直接问答(1-20) / B 对比题(21-24) / C 跨段综合(25-28) / D 库外拒答(29-30) / E 口语化改写(31-39)
# 覆盖全部 11 主题，reference 均可从知识库对应段落溯源（出处见注释）
# E 类为 Query 改写层评测专用：口语问法 + 规范 reference，测改写前后检索差异
TEST_SET = [
    # ========== A 直接问答（20 题） ==========
    # sample：机器学习
    {"question": "机器学习分为哪几类学习方式？",
     "reference": "分为三类：监督学习（用带标签数据训练，如分类、回归）、无监督学习（从无标签数据中发现结构，如聚类）、强化学习（智能体与环境交互、用奖励信号学习策略）。"},
    # sample：过拟合
    {"question": "什么是过拟合？如何解决？",
     "reference": "过拟合指模型在训练集上表现很好、但在未见过的测试集上表现差，原因是过度记忆了训练数据中的噪声。解决：正则化（L1/L2）、dropout、增加数据、早停、交叉验证。"},
    # kb_01-4：位置编码
    {"question": "什么是位置编码？为什么 Transformer 需要它？有哪些实现方式？",
     "reference": "Transformer 靠注意力机制并行处理所有词，本身不带顺序信息，必须额外注入位置信息。实现方式：正弦余弦位置编码（固定三角函数，不需训练）、可学习位置编码（BERT 等，训练中学出）、旋转位置编码 RoPE（按位置旋转词向量，能表达相对距离，LLaMA 等主流模型常用）。"},
    # kb_01-1：词元化
    {"question": "什么是词元化？BPE 和 WordPiece 有什么区别？",
     "reference": "词元化是把文本切成 token（模型处理的最小单位）再编号。BPE 和 WordPiece 都是子词切分算法：从单个字符出发，反复合并高频相邻片段。区别：BPE 合并标准是出现频率最高者优先，GPT 系列使用；WordPiece 用概率衡量合并收益（让模型更不吃惊），BERT 系列使用，切出的子词常带 ## 前缀。"},
    # kb_03-2：RAG 流水线
    {"question": "一个完整的 RAG 流水线包含哪些关键步骤？",
     "reference": "四步：①数据准备加切块（清洗文档、切成合适大小的 chunk）；②向量化加入库（用 embedding 模型把文本转成向量，存进向量数据库）；③检索（把用户问题转向量，找语义最相近的几段，可加 Rerank 重排）；④拼接加生成（把检索资料和问题组装成提示词，让大模型基于资料生成答案，通常附来源出处）。"},
    # kb_03-6：Lost in the Middle
    {"question": "什么是 Lost in the Middle 问题？如何缓解？",
     "reference": "模型处理长上下文时对首尾内容利用好、对中间内容容易忽略（准确率呈 U 形），关键信息放中间会导致回答质量下降。缓解：①重排序让最相关的段落放首尾；②少喂精喂，只保留 top 几条最相关段落；③分批处理（map-reduce）或换长上下文更稳的模型，配合引用标注。"},
    # kb_02-1：Agent 组件
    {"question": "一个 LLM 智能体由哪些核心组件构成？",
     "reference": "核心组件：①大脑（LLM，负责理解任务、推理、决策）；②规划（把大目标拆成小步骤并根据结果调整）；③工具（调用搜索引擎、计算器、数据库等外部能力）；④记忆（短期记忆是当前对话上下文，长期记忆用向量数据库等存储重要信息）。"},
    # kb_02-2：ReAct
    {"question": "什么是 ReAct 框架？",
     "reference": "ReAct 是 Reasoning（推理）加 Acting（行动）的结合：让 AI 边思考边行动，按 Thought（想）→ Action（做，调用工具）→ Observation（看结果）三步循环，直到任务完成。最大好处是少编造、可纠错，是 Agent 的基础工作模式。"},
    # kb_05-2：few-shot
    {"question": "什么是 few-shot 提示？",
     "reference": "few-shot 是给模型 2 到 5 个示例再让它完成任务，例子覆盖典型和边界情况，准确率和稳定性最高，代价是提示词更长、更费 token。实战上先用 zero-shot 试，效果不稳再加例子。"},
    # kb_06-3：LoRA
    {"question": "什么是 LoRA？",
     "reference": "LoRA（低秩适配）是参数高效微调方法：冻结原模型参数不动，只在旁边新增一组很小的可训练参数，把大改动拆成两个小矩阵相乘来近似（即低秩），训练只更新这一小部分。LoRA 文件通常几 MB 到几十 MB，可即插即用、随时切换，一张普通显卡就能跑。"},
    # kb_06-8：RLHF
    {"question": "什么是 RLHF？",
     "reference": "RLHF（基于人类反馈的强化学习）分三步：①预训练加有监督微调（SFT）得到会说话的模型；②让人类对同一问题的多个回答排序，用偏好数据训练奖励模型；③用强化学习让模型追求高分，使行为对齐人类偏好，更有帮助、诚实、安全。"},
    # kb_07-1：MCP
    {"question": "什么是 MCP？",
     "reference": "MCP（Model Context Protocol）是 Anthropic 提出的开放标准，给 AI 与外部工具、数据定义统一接口，像 AI 世界的 USB。角色分 Server（把工具或数据源包装成标准接口）和 Client（嵌在 AI 应用里调用），通过 JSON-RPC 通信，标准化三类能力：Tools、Resources、Prompts。好处是工具写一次所有 AI 都能用，避免 N 乘 M 套重复对接代码。"},
    # kb_07-6：Agent 循环
    {"question": "什么是 Agent 循环？它是如何决定停止的？",
     "reference": "Agent 循环是思考→行动→观察三步反复转圈的模式：每轮 AI 先决定下一步（可调用工具），拿到结果再进入下一轮。停止条件：①AI 认为任务完成，直接输出最终回答；②硬性上限（最大迭代次数、超时、token 超限）强制中断；③工具报错、连续无进展或人为喊停。"},
    # kb_08-1：KV cache
    {"question": "什么是 KV cache？",
     "reference": "KV cache 是缓存 Transformer 注意力计算中历史 token 的 Key 和 Value。生成每个新 token 都要回头看前面所有 token，不缓存就得重算全部 KV，计算量随长度暴涨；缓存后历史 KV 只算一次，新 token 只算自己那份再查缓存。本质是拿空间（显存）换时间，长对话时缓存越来越大、吃显存。"},
    # kb_08-2：量化
    {"question": "什么是模型量化？",
     "reference": "量化是把模型参数从高精度（如 FP32）转成低精度（如 INT8）存储。位数变少后模型文件变小（32 位降 8 位理论缩到约四分之一）、推理更快更省电。代价是精度降太狠（如 4 位以下）可能让回答质量下降，所以常只在推理阶段量化、训练仍用高精度。"},
    # kb_09-8：混合检索（项目核心）
    {"question": "什么是混合检索？为什么它比纯向量检索更好？",
     "reference": "混合检索是关键词检索（BM25，精确命中专有名词、编号、罕见词）和向量检索（语义相似，理解换说法、同义词）两路同时检索，再用 RRF 等方法融合排序。比纯向量好的原因：向量只看语义，遇到型号、人名、缩写等必须精确的词容易匹配不到或召回不相关内容；混合补位后召回更全、排序更准，是 RAG 的关键优化。"},
    # kb_09-2：稀疏 vs 稠密
    {"question": "稀疏向量和稠密向量有什么区别？",
     "reference": "稠密向量维度低（几百到几千）、几乎每格都有值，由神经网络生成，擅长语义检索（找意思相近）；稀疏向量维度极高（上万，约等于词表大小）、绝大多数是 0，由词频统计（TF-IDF、BM25）或专门模型生成，擅长关键词精确匹配。RAG 中常把两者结合成混合检索。"},
    # kb_10-3：CLIP
    {"question": "CLIP 是如何工作的？",
     "reference": "CLIP 用对比学习把图像和文本对齐：一个图像编码器加一个文本编码器，把图和句子变成同一空间里的向量；训练时让配对的图文向量靠得近、错误配对拉得远（用约 4 亿对图文数据，无人工精细标注）。实际应用是零样本分类：把候选句子都编码，看图片向量和谁最近就选谁。"},
    # kb_04-1：BLEU/ROUGE 局限
    {"question": "为什么 BLEU、ROUGE 这类传统指标评估 LLM 生成质量有很大局限？",
     "reference": "BLEU/ROUGE 本质是对答案：统计 n-gram 字面重合度。局限：①只看字面重合、看不懂语义，同义改写会被判低分；②开放任务答案不唯一，拿少量参考答案比对不公平；③测不了事实错误（幻觉）、逻辑、是否帮助用户等质量维度；④分数常常和人类判断对不上。"},
    # kb_11-1：幻觉
    {"question": "什么是幻觉？RAG 检索到正确上下文却还在幻觉，该怎么修？",
     "reference": "幻觉是大模型一本正经地编造听起来合理但错误的内容。检索正确还幻觉的原因：资料太长、关键信息埋中间（lost in the middle）、提示词没约束、资料与问题对不上、模型凭记忆发挥。修法：①精简上下文加重排序，把最相关放前面；②提示词明确只能根据资料回答、没有就说不知道、标引用；③检查检索质量、资料加时间来源标注；④生成后做忠实度校验，逐句比对资料、对不上打回重写。"},
    # ========== B 对比题（4 题） ==========
    # kb_06-11：微调/RAG/提示选择
    {"question": "微调、RAG、提示工程三者到底该怎么选？",
     "reference": "一句话：缺怎么说用提示工程（最便宜最快，改指令即可，永远先试）；缺知识用 RAG（适合知识常变、内容私有的场景，还能减少幻觉）；缺风格或能力才用微调（改变说话风格和本能反应，但要准备数据、花钱花时间，知识更新了还得重练）。三者常常组合使用，比如微调出语气、再用 RAG 喂最新知识。"},
    # kb_01-10：MHA/MQA/GQA
    {"question": "MHA、MQA、GQA 有什么区别？",
     "reference": "三者差在 K/V 是否共享：注意力里每个头都有 Q、K、V，MHA 每个头独立 Q/K/V，最准但最费内存；MQA 所有头共享一份 K/V、只有 Q 独立，最省内存但质量损失明显；GQA 把头分组、组内共享 K/V，是折中，效果接近 MHA 还省一大半内存。省 K/V 是因为 KV 缓存特别吃显存、拖慢速度，主流模型（如 Llama 大号版本）多用 GQA。"},
    # kb_02-6：LangChain vs LlamaIndex
    {"question": "LangChain 和 LlamaIndex 有什么不同？",
     "reference": "LlamaIndex 专注 RAG：把文档切块、存向量库、精准检索这条链路做得最细，适合从大量文档查准答案的场景；LangChain 专注编排：把多步骤、工具、模型调用串成一条流程，适合做 Agent 等复杂应用。不是二选一，可配合使用——LlamaIndex 负责检索，LangChain 把检索结果接进更大的流程。"},
    # kb_03-8：RAG vs 搜索
    {"question": "RAG 和传统搜索系统有什么区别？",
     "reference": "搜索系统建好索引、按相关度排序，输出链接或文档列表，不负责理解内容，由用户自己消化；RAG 是检索加生成两步走：第一步用搜索（常是向量检索）找资料，第二步把资料喂给大模型生成连贯回答。最大区别：搜索给你一堆资料，RAG 给你一个答案；RAG 里的搜索是手段、生成才是终点，还能减少幻觉、附上出处。"},
    # ========== C 跨段综合（4 题） ==========
    # kb_03-7 + kb_04：RAG 评估
    {"question": "如何全面评估一个 RAG 系统的性能？",
     "reference": "分两个阶段：检索阶段看召回率（该找到的相关资料找回几成）、精确率（找回的资料里真正有用的占几成）、命中率或排名（正确资料是否排在前几条）；生成阶段看答案相关性（是否回应了问题）和忠实度或事实一致性（是否基于检索资料、有没有编造，专门抓幻觉）。常用 LLM-as-a-judge 或 RAGAS 工具自动计算，还可加端到端指标：用户满意度、延迟、成本。"},
    # kb_08-9 + kb_11-1：LLMOps
    {"question": "LLM 应用上线后如何监控和维护？",
     "reference": "LLMOps 是把 AI 应用从开发、上线到长期维护串起来的流程，核心是持续管理提示词、成本、效果和版本。上线后监控四类指标：质量（回答准不准、有没有幻觉）、性能（延迟）、成本（按 token 计费）、安全（防止泄露）。性能衰退往往悄悄发生，要留痕加抽查（记录用户提问和回答的日志、定期抽查）；应对手段：回退旧版本、优化提示词、补充更准的参考资料（RAG）。"},
    # kb_02-1 + kb_07 + kb_11-2：构建 Agent
    {"question": "从 0 构建一个智能体应用需要哪些核心组件？",
     "reference": "核心组件：①LLM 大脑（理解任务、推理决策）；②规划（拆解目标、根据结果调整计划）；③工具（搜索、计算器、数据库等，通过 Function Calling 或 MCP 接入）；④记忆（短期上下文加长期向量库）；⑤安全护栏（输入输出过滤、权限最小化、高危操作人工确认 human-in-the-loop、全程留痕可回滚）；⑥Agent 循环（思考-行动-观察，带停止条件和最大次数上限）。"},
    # kb_05-6 + kb_11-2/9/10：安全风险
    {"question": "如何防止 LLM 应用中的安全风险？",
     "reference": "四类防护：①提示注入防御——把可信指令和不可信数据明确分开（用特殊标记包裹用户内容）、对输入做过滤检测；②护栏与权限——高危操作（删数据、转账）强制人工确认（human-in-the-loop）、权限按最小够用配置、加用量上限；③隐私防护——训练数据先脱敏、去重或差分隐私、输出端过滤敏感格式内容、别把机密写进 prompt；④系统提示泄露防护——真机密别放提示里（放后端用程序控制）、加不得透露提示的指令加输出过滤、检测越狱和注入话术，并按敏感度分级管理。"},
    # ========== D 库外拒答（2 题） ==========
    {"question": "怎么做红烧肉？",
     "reference": "抱歉，知识库中未找到相关内容。"},
    {"question": "请介绍北京烤鸭的完整做法",
     "reference": "抱歉，知识库中未找到相关内容。"},
    # ========== E 口语化改写（9 题，Query 改写层评测用） ==========
    # 口语问法与知识库术语词面差距大，改写层收益体现在这类题上
    # kb_02-1：多轮对话记忆
    {"question": "AI 咋记住之前说过的话？",
     "reference": "靠短期记忆和长期记忆：短期记忆是对话历史存在上下文窗口，窗口有限，用滑动窗口保留最近几轮、对更早内容做摘要压缩；长期记忆把重要信息存外部存储（向量数据库），需要时语义检索召回；还可把全部历史对话存入向量库、每轮只检索最相关的几条（RAG 记忆），或把用户偏好、关键事实单独抽成结构化记忆。换更大窗口的模型可缓解，但成本和速度会增加。"},
    # kb_03-2：RAG 原理
    {"question": "帮我看下那个 RAG 到底咋回事",
     "reference": "RAG（检索增强生成）是先去资料库检索与问题相关的段落，再让大模型基于检索资料生成回答。完整流水线四步：①数据准备与切块（清洗文档、切成合适 chunk）；②向量化入库（embedding 把文本转向量、存进向量数据库）；③检索（问题转向量、找语义最相近的几段，可加 Rerank 重排）；④拼接生成（把检索资料和问题组装成提示词，模型基于资料回答并附来源）。"},
    # kb_06-11：微调/RAG/提示选择（成本视角）
    {"question": "RAG 是不是比微调划算",
     "reference": "从成本看 RAG 通常更划算：不改模型参数，只需加检索和提示词工程部分，成本低、上手快、性价比高；微调要准备数据、花钱花时间，知识更新了还得重新训练。选择原则：缺怎么说用提示工程（最便宜、永远先试），缺知识用 RAG，缺风格或能力才用微调，三者常组合使用。"},
    # kb_09-8 / kb_09-2：向量与语义检索
    {"question": "向量是咋存的，咋就找得到差不多的",
     "reference": "文本先用 embedding 模型转成向量（稠密向量，几百到几千维，由神经网络生成，语义相近的文本向量也接近），再存进向量数据库（如 Chroma）；检索时把用户问题也转向量，按语义相似度找最相近的段落，因此能理解换说法、同义词。纯向量检索的弱点是遇到型号、人名、缩写等必须精确的词容易漏，常与 BM25 关键词检索组合成混合检索。"},
    # sample：过拟合 + kb_06：灾难性遗忘
    {"question": "模型老学一样的东西会不会学坏",
     "reference": "会。反复学同样的数据容易过拟合：在训练集上表现很好、在未见过的测试集上表现差，原因是过度记忆了训练数据里的噪声。解决：正则化（L1/L2）、dropout、增加数据、早停、交叉验证。微调场景还要防灾难性遗忘——学新知识时覆盖掉原有知识，需小心调整参数、混合旧数据。"},
    # kb_07-1：MCP + kb_02-2：ReAct
    {"question": "咋让 AI 自己动手调工具",
     "reference": "通过工具调用（Function Calling）或 MCP（Model Context Protocol）：MCP 给 AI 与外部工具、数据定义统一接口（Server 把工具包装成标准接口，Client 嵌在 AI 应用里调用，JSON-RPC 通信，标准化 Tools/Resources/Prompts 三类能力），工具写一次所有 AI 都能用。Agent 循环里 AI 按 Thought→Action（调工具）→Observation 循环执行。"},
    # kb_05-2：few-shot 提示
    {"question": "prompt 咋写才能让模型听话点",
     "reference": "提示工程核心：指令明确具体、说清期望输出格式；任务复杂时用 few-shot 给 2 到 5 个示例、覆盖典型和边界情况，准确率和稳定性最高（代价是提示词更长更费 token）；实战先 zero-shot 试，效果不稳再加例子；对话类应用还可设定角色和回答规范（如拒答规则、引用规则）。"},
    # kb_08-1 / kb_08-2：推理优化
    {"question": "模型上线了咋让它跑快点",
     "reference": "常见手段：①KV cache——缓存历史 token 的 Key 和 Value，避免每生成一个新 token 都重算全部，本质拿显存换速度；②量化——参数从 FP32 降到 INT8 等低精度，模型文件变小、推理更快更省电（训练仍用高精度）；③精简上下文长度也能直接降低计算量。"},
    # kb_04-1 + kb_03-7：LLM 评估
    {"question": "咋知道模型答得对不对，光看它自己说行吗",
     "reference": "不能只看模型自说自话，要用评测。传统 BLEU/ROUGE 对答案看字面重合，局限大：看不懂语义、开放任务答案不唯一、测不了幻觉。更有效的是分维度评测：忠实度（是否基于资料、有没有编造）、答案相关性、检索召回、检索精确度，用 LLM-as-a-judge 或 RAGAS 自动计算；上线后还要持续监控质量、性能、成本、安全四类指标。"},
]
