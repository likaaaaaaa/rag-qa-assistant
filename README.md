# AI 领域知识库 RAG 问答助手

基于本地知识库的大模型检索增强生成（RAG）应用：**211 段 AI 领域知识库 · 混合检索 + Rerank 优化 · RAGAS 量化评测 · Streamlit 已部署上线**。

> 🔗 **在线体验**:https://rag-app-assistant-lp6fulldtxdpde5ba4f5eg.streamlit.app

## 功能亮点

- **知识库**：11 大主题（机器学习 / 深度学习 / 大模型 / RAG / Agent / Prompt / 微调 / 向量 / 部署 / 评估 / 多模态），211 段内容（课程知识 + 开源资料整理）；
- **检索优化（三模式可插拔）**：
  - `vector`：纯向量检索（基线）
  - `hybrid`：向量 + BM25（jieba 中文分词）→ RRF 融合
  - `hybrid_rerank`：混合检索候选 → 智谱 Rerank 精排 → top-5
- **量化评测**：自建测试集（39 题：直接/对比/跨段/拒答/口语化）+ RAGAS 四指标（忠实度/相关度/召回/精确度）；
- **优化成果**：
  - 14 题旧库：检索精确度 0.75 → **0.89（+19%）**，忠实度保持不降；
  - 30 题新库复测：混合检索精确度 0.861 → **0.894**、召回 0.856 → **0.933**，剔除拒答口径精确度 0.923 → **0.958**；
- 拒答约束 + **可验证 RAG（生成后逐句自检，无依据内容自动移除/拒答）**，答案不编造；
- **多轮对话记忆**：检索前指代消解（"它"→具体概念），最近 4 轮窗口；
- **引用溯源**：顺序编号 + 可点击跳转 + 完整段落引用区，所见即所据；
- 聊天 UI：消解/检索/生成耗时显示、检索模式三档切换（侧边栏）。

## 更新日志

- **2026-08-10 · v1.3 可验证 RAG + 多轮记忆**
  - 多轮记忆：检索前指代消解（"它跟微调比呢"→"RAG跟微调比呢"），6/6 多轮场景验证通过；
  - 可验证 RAG：回答生成后逐句自检，无依据句子自动移除或拒答，忠实度防线加固；
  - 引用溯源升级：顺序编号 + 可点击跳转 + 完整段落引用区（所见即所据）；
  - 检索 top-3 → top-5；评测脚本新增子集模式（--subset e，省 API）；
  - Query 改写层实验评估：三轮调优数据为负收益（基线召回已达 0.92 无增益空间），已关闭并记录优化边界。
- **2026-08-09 · v1.2 30 题新库评测 + 提示词升级 + UI 优化**
  - 30 题三档评测全跑完：混合检索召回 0.856→**0.933**、精确度 0.861→**0.894**；
  - 提示词拒答/引用 3 处强化（抽样验证修复拒答失效、编造、漏引用）；
  - 聊天 UI：思考中状态、耗时显示、引用溯源代码补全、历史消息修复。
- **2026-08-08 · v1.1 知识库扩建**
  - 知识库 11 → **211 段**（11 主题，开源资料导入，convert_kb.py 可复用）；
  - 30 题测试集落库（20 直接 / 4 对比 / 4 跨段 / 2 拒答，reference 可溯源）。
- **2026-08-07 · v1.1 检索优化**
  - 混合检索（向量+BM25 RRF）：精确度 0.750→**0.845**；
  - Rerank 精排：0.845→**0.893**（+19%，14 题旧库）。
- **2026-08-06 · v1.0 上线**
  - RAG 问答助手 v1 部署上线（Streamlit Cloud）；RAGAS 评测基线搭建（14 题）。

## 架构

```
用户提问 → 混合检索(向量+BM25, Top-10) → RRF 融合 → Rerank 精排 → Top-5 上下文 → 回答自检 → 引用标注
文档加载 → 切分(500/50) → embedding-2 分批向量化 → Chroma 持久化 → 检索 → 智谱 glm-4-flash 生成
```

## 技术栈

LangChain · Chroma · 智谱 AI（glm-4-flash / embedding-2 / Rerank）· BM25 · jieba · RAGAS · Streamlit

## 本地运行

```bash
pip install -r requirements.txt
echo "ZHIPU_API_KEY=xxx" > .env
streamlit run app.py        # 或命令行体验：python rag.py
```

## 评测

```bash
python eval_ragas.py --mode vector           # 基线
python eval_ragas.py --mode hybrid           # 混合检索
python eval_ragas.py --mode hybrid_rerank    # 混合 + Rerank
```
